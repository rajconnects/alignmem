# internal/

Maintainer-only assets that are NOT shipped to end users.

- `schema-diff/` — V1 SaaS DB schema migrations (graph readiness, etc.). Reference for syncing the public DTP protocol shape with the internal Alignmink-V1 schema. Not part of the CEO-facing engine; excluded from the npm package and from `engine.zip`.
